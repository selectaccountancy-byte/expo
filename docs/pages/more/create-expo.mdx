// Project: Select Accountancy App (Expo React Native starter)
// Files included below. Copy each file into your Expo project.

// package.json
{
  "name": "select-accountancy-app",
  "version": "1.0.0",
  "main": "node_modules/expo/AppEntry.js",
  "scripts": {
    "start": "expo start",
    "android": "expo run:android",
    "ios": "expo run:ios",
    "web": "expo start --web"
  },
  "dependencies": {
    "expo": "~48.0.0",
    "react": "18.2.0",
    "react-native": "0.71.8",
    "@react-navigation/native": "^6.1.6",
    "@react-navigation/native-stack": "^6.9.12",
    "expo-status-bar": "~1.4.4",
    "react-native-safe-area-context": "4.5.0",
    "react-native-screens": "^3.20.0",
    "@react-native-async-storage/async-storage": "^1.17.11"
  }
}

// README.md
# Select Accountancy - Expo Starter

This is a minimal Expo React Native starter app I created for you.

## Features included
- Splash + simple header
- Email/password mock Login (no backend) with AsyncStorage
- Home screen with a simple card list
- Contact screen with a form (sends to device email composer placeholder)
- Basic navigation (stack)

## How to run
1. Install dependencies: `npm install` or `yarn`
2. Start Expo: `expo start`
3. Use Expo Go (Android/iOS) to preview or run on a simulator.

## Build
Use EAS (Expo Application Services) or `expo run:android` / `expo run:ios` (requires native toolchains).


// app.json (minimal)
{
  "expo": {
    "name": "Select Accountancy",
    "slug": "select-accountancy-app",
    "version": "1.0.0",
    "sdkVersion": "48.0.0",
    "platforms": ["ios","android","web"],
    "orientation": "portrait",
    "icon": "./assets/icon.png",
    "splash": {
      "image": "./assets/splash.png",
      "resizeMode": "contain",
      "backgroundColor": "#2563EB"
    }
  }
}

// /App.js
import React, { useState, useEffect } from 'react';
import { NavigationContainer } from '@react-navigation/native';
import { createNativeStackNavigator } from '@react-navigation/native-stack';
import { SafeAreaView, View, Text, Button, TextInput, StyleSheet, TouchableOpacity, FlatList, Alert } from 'react-native';
import AsyncStorage from '@react-native-async-storage/async-storage';
import { StatusBar } from 'expo-status-bar';

const Stack = createNativeStackNavigator();

function SplashScreen({ navigation }) {
  useEffect(() => {
    const timer = setTimeout(async () => {
      const user = await AsyncStorage.getItem('user');
      navigation.replace(user ? 'Home' : 'Login');
    }, 800);
    return () => clearTimeout(timer);
  }, []);

  return (
    <SafeAreaView style={styles.center}>
      <Text style={styles.title}>Select Accountancy</Text>
      <Text style={{ marginTop: 8 }}>Your finance & accounting helper</Text>
      <StatusBar style="auto" />
    </SafeAreaView>
  );
}

function LoginScreen({ navigation }) {
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');

  const login = async () => {
    if (!email || !password) return Alert.alert('Please enter email and password');
    // Mock login: accept any credentials
    await AsyncStorage.setItem('user', JSON.stringify({ email }));
    navigation.replace('Home');
  };

  return (
    <SafeAreaView style={styles.container}>
      <Text style={styles.title}>Welcome</Text>
      <TextInput placeholder="Email" value={email} onChangeText={setEmail} style={styles.input} keyboardType="email-address" autoCapitalize="none" />
      <TextInput placeholder="Password" value={password} onChangeText={setPassword} style={styles.input} secureTextEntry />
      <TouchableOpacity style={styles.button} onPress={login}>
        <Text style={styles.buttonText}>Log in</Text>
      </TouchableOpacity>
      <View style={{ marginTop: 16 }}>
        <Button title="Open Demo Home" onPress={() => navigation.replace('Home')} />
      </View>
    </SafeAreaView>
  );
}

function HomeScreen({ navigation }) {
  const data = [
    { id: '1', title: 'Invoices', subtitle: 'Create and manage invoices' },
    { id: '2', title: 'GST Reports', subtitle: 'Quick GSTR summary' },
    { id: '3', title: 'Clients', subtitle: 'Client list & contacts' },
  ];

  const logout = async () => {
    await AsyncStorage.removeItem('user');
    navigation.replace('Login');
  };

  return (
    <SafeAreaView style={styles.container}>
      <View style={styles.headerRow}>
        <Text style={styles.title}>Dashboard</Text>
        <TouchableOpacity onPress={() => navigation.navigate('Contact')}>
          <Text style={{ color: '#2563EB' }}>Contact</Text>
        </TouchableOpacity>
      </View>

      <FlatList
        data={data}
        keyExtractor={(i) => i.id}
        renderItem={({ item }) => (
          <TouchableOpacity style={styles.card} onPress={() => Alert.alert(item.title)}>
            <Text style={styles.cardTitle}>{item.title}</Text>
            <Text style={styles.cardSubtitle}>{item.subtitle}</Text>
          </TouchableOpacity>
        )}
      />

      <View style={{ marginTop: 12 }}>
        <Button title="Logout" onPress={logout} color="#ef4444" />
      </View>
    </SafeAreaView>
  );
}

function ContactScreen() {
  const [name, setName] = useState('');
  const [message, setMessage] = useState('');

  const submit = () => {
    if (!name || !message) return Alert.alert('Please fill name and message');
    Alert.alert('Message ready to send', `Name: ${name}\nMessage: ${message}`);
  };

  return (
    <SafeAreaView style={styles.container}>
      <Text style={styles.title}>Contact Us</Text>
      <TextInput placeholder="Your name" value={name} onChangeText={setName} style={styles.input} />
      <TextInput placeholder="Message" value={message} onChangeText={setMessage} style={[styles.input, { height: 120 }]} multiline />
      <TouchableOpacity style={styles.button} onPress={submit}>
        <Text style={styles.buttonText}>Send</Text>
      </TouchableOpacity>
    </SafeAreaView>
  );
}

export default function App() {
  return (
    <NavigationContainer>
      <Stack.Navigator screenOptions={{ headerShown: false }}>
        <Stack.Screen name="Splash" component={SplashScreen} />
        <Stack.Screen name="Login" component={LoginScreen} />
        <Stack.Screen name="Home" component={HomeScreen} />
        <Stack.Screen name="Contact" component={ContactScreen} />
      </Stack.Navigator>
    </NavigationContainer>
  );
}

const styles = StyleSheet.create({
  center: { flex:1, justifyContent:'center', alignItems:'center' },
  container: { flex:1, padding:20 },
  title: { fontSize:26, fontWeight:'700', color:'#111827' },
  input: { borderWidth:1, borderColor:'#E5E7EB', padding:12, borderRadius:8, marginTop:12 },
  button: { backgroundColor:'#2563EB', padding:12, borderRadius:8, alignItems:'center', marginTop:16 },
  buttonText: { color:'#fff', fontWeight:'600' },
  headerRow: { flexDirection:'row', justifyContent:'space-between', alignItems:'center' },
  card: { padding:14, borderRadius:10, backgroundColor:'#F8FAFC', marginTop:12 },
  cardTitle: { fontWeight:'700' },
  cardSubtitle: { color:'#6B7280', marginTop:4 }
});

// assets placeholders
// Add your images at /assets/icon.png and /assets/splash.png

// Notes & Next steps
// - Replace mock login with real auth (Firebase/Auth0/custom) when ready.
// - Provide app name, logo (1024x1024), and primary color if you want custom branding. I used #2563EB.
// - To build for stores, use EAS: `eas build -p android` and `eas build -p ios` (iOS requires Apple Developer account).

// Store listing draft (paste to Play Store / App Store):
// App name: Select Accountancy
// Short description: Simple accounting assistant for small businesses
// Long description: Select Accountancy helps small business owners manage invoices, view GST summaries, and keep client contacts in one place. Fast, secure starter app with easy-to-use dashboard and contact support.

// End of project starter

